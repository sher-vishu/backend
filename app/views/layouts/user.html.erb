<!DOCTYPE html>
<html>
  <head>
    <title>Devise with Rails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application.tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <main class="flex">
      <div class="flex w-full">
        <nav class="w-64 p-4 h-screen bg-gray-600 sticky top-0">
          <div class="flex">
            <h1 class="text-md py-2 text-white font-bold hidden md:block">User</h1>
          </div>
          <ul class="flex flex-col text-white">
            <li>
              <%= link_to users_path, class: "block px-4 py-2 hover:rounded hover:bg-gray-700" do %>
                 <span class="hidden md:inline-block">Your AI Models</span>
              <% end %>
            </li>
            <li>
              <%= link_to associate_ai_models_form_path, class: "block px-4 py-2 hover:rounded hover:bg-gray-700" do %>
                <span class="hidden md:inline-block">Select AI Models</span>
              <% end %>
            </li>
            <li>
              <%= link_to destroy_user_session_path, data: { "turbo-method": :delete, "turbo-confirm": "Are you sure you want to sign out?" }, class: "block px-4 py-2 hover:rounded hover:bg-gray-700" do %>
                <span class="hidden md:inline-block">Sign Out</span>
              <% end %>
            </li>
          </ul>
        </nav>
        <div class="w-full p-8">
          <%= yield %>
        </div>
      </div>
    </main>
  </body>
</html>
